# MEA (Median–Extremes Alternation)

This repository contains the reference implementation of the MEA permutation:
a deterministic, reversible reordering algorithm defined by alternating
extractions from the center and the edges of a sequence.

See `mea.py` for the implementation.
